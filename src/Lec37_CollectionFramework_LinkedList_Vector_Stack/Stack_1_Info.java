package Lec37_CollectionFramework_LinkedList_Vector_Stack;



/*
The stack is the subclass of Vector. 
models and implements Stack data structure
100% capacity increased
***It implements the last-in-first-out data structure, i.e., Stack. by using search() we can see the position based -1 policy
The stack contains all of the methods of Vector class and also provides its methods like boolean push(), boolean peek(), boolean push(object o), which defines its properties.
Methods in Stack class
1.Object push(Object element) :🡪to Add element (Pushes an element on the top of the stack.)
2.Object pop() :🡪to delete element( Removes and returns the top element of the stack. An ‘EmptyStackException’ exception is thrown if we call pop() when the invoking stack is empty.)
3.Object peek() :-> to find Element Returns the element on the top of the stack, but does not remove it.
4.boolean empty() : It returns true if nothing is on the top of the stack. Else, returns false.
5. int search(Object element) : It determines whether an object exists in the stack. If the element is found, it returns the position of the element from the top of the stack. Else, it returns -1.*/




//Topics to be covered:

//Basic ----create Stack and add elements....print

//Use of Methods: push,pop,peak.search etc

//use method for condition (if possible)









public class Stack_1_Info {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
